---

name: "Auto Feature Branch PR"
on:
  pull_request:
    branches:
      - 'test*'
jobs:
  open_pr:
    runs-on: macos-10.15
    steps:
      - run: |
          "${GITHUB_WORKSPACE}/.github/workflows/scripts/open_pr.sh ${GITHUB_API_URL} ${GITHUB_REF}"
